
version: 2

models:
    - name: product
      description: "product information"
      columns:
          - name: products_id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
          - name: purchSE_PRICE
